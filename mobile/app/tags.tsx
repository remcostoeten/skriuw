import { RoutePlaceholder } from "@/shell/route-views";

export default function TagsRoute() {
  return (
    <RoutePlaceholder
      route="tags"
      detail="Browsing tags is not on mobile yet. Search for #tag or tag:name to find the notes carrying one."
    />
  );
}
