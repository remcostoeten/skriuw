import { RoutePlaceholder } from "@/shell/route-views";

export default function TagsRoute() {
  return (
    <RoutePlaceholder
      route="tags"
      detail="Tags and their notes arrive with the search surface."
    />
  );
}
