import { RoutePlaceholder } from "@/shell/route-views";

export default function PeopleRoute() {
  return (
    <RoutePlaceholder
      route="people"
      detail="People and their notes arrive with the search surface."
    />
  );
}
