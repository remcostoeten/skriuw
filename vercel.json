{
  "buildCommand": "pnpm -r --filter '@skriuw/db' build && cd apps/web && pnpm build",
  "outputDirectory": "dist/spa",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "VITE_STORAGE_ADAPTER": "serverless-api"
  }
}
