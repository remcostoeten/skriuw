# Lovable export archives
lovable-*.zip

# Test coverage reports (generated by scripts/)
coverage.json
coverage.md
coverage/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Rust / Tauri build artifacts (v1/apps/desktop/src-tauri/target ~ 5GB)
target/
project-management/
*.local
.env
.env.*
!.env.example

# Cloudflare Workers / wrangler dev secrets (collab PartyServer)
.dev.vars
.dev.vars.*
.wrangler/
.partykit/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.next/
.expo/
.fix-filenames-manifest.json
.vercel
test-results/
*.tsbuildinfo

# React Doctor reports (generated by scripts/)
react-doctor-reports/

# Prisma generated client (regenerated via `bunx prisma generate`)
# Output now lives at v1/apps/web/generated; keep unanchored so it's ignored there.
generated/

# Transport contracts are generated but intentionally committed.
!contracts/generated/
!contracts/generated/*.schema.json

# Icon data is generated from @fluentui/svg-icons and committed so the drift check can compare it.
!packages/icons/generated/

TODO.*
.env*.local
.pnpm-store/
.claude/
.agents/
.env*
!.env.example
!v1/apps/mobile/.env.example
v1/apps/desktop/skriuw_*_amd64-working-directory/

next-beats/
paralelprompts/
v1/apps/documentation/native/

# One-shot release-notes files fed to gh release create
release-notes-v2-*.txt

# v2 core (repository root since the v2 hoist)
/.build/
/.data/
*.db
*.db-shm
*.db-wal
/apps/workspace/e2e/results/

# Local MCP config; holds machine-absolute paths
/.mcp.json

