{
	"name": "skriuw-desktop",
	"version": "0.24.0",
	"private": true,
	"scripts": {
		"dev": "tauri dev",
		"build": "cross-env NO_STRIP=1 tauri build",
		"build:release": "cross-env NO_STRIP=1 tauri build --config src-tauri/tauri.release.conf.json",
		"build:snap": "bun run build && snapcraft pack",
		"tauri": "tauri"
	},
	"devDependencies": {
		"@tauri-apps/cli": "^2.11.0",
		"cross-env": "^7.0.3"
	}
}
