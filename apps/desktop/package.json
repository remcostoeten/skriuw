{
	"name": "skriuw-desktop",
	"version": "0.15.0",
	"private": true,
	"scripts": {
		"dev": "cross-env WEBKIT_DISABLE_DMABUF_RENDERER=1 tauri dev",
		"build": "cross-env NO_STRIP=1 tauri build",
		"build:snap": "bun run build && snapcraft pack",
		"tauri": "tauri"
	},
	"devDependencies": {
		"@tauri-apps/cli": "^2.11.0",
		"cross-env": "^7.0.3"
	}
}
