## Context Menu Keyboard Shortcuts

When we right click on a folder or file in the sidebar, we want to be able to use keyboard shortcuts to interact with the context menu.

Each item already has a visual indicator, which is  asingle letter. For example rename has the kbd of `R`.  Deleet note should be `Ctrl`/`CMD` + `Backspace`.

New folder should be `F`        

New note should be `N`

Currently when u press that letter it does focus the input as it is the first match, but w  e really want to execute it directly.

Make sure the system uses the same keyboard shortcuts system we have in our entire applicatin. No custom logic should be used.
i on