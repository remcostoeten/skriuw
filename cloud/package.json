{
  "name": "@skriuw/cloud",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.20.1",
    "@cloudflare/workers-types": "5.20260801.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.118.0"
  },
  "private": true,
  "scripts": {
    "types": "wrangler types worker-configuration.d.ts",
    "types:check": "bash scripts/without-dev-vars.sh wrangler types worker-configuration.d.ts --check",
    "typecheck": "tsc --noEmit",
    "test": "bash scripts/without-dev-vars.sh vitest run",
    "check": "bun run types:check && bun run typecheck && bun run test",
    "deploy:dry": "wrangler deploy --dry-run"
  },
  "type": "module",
  "dependencies": {
    "@better-auth/infra": "0.4.1",
    "@cfworker/json-schema": "4.1.1",
    "better-auth": "1.6.26"
  }
}
