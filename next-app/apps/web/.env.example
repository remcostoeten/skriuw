# Database connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Optional: Force a specific database provider (auto-detected by default)
# DATABASE_PROVIDER=neon | postgres
