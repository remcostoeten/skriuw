# Next Session Notes

- Branch: `feature/storage`.
- Task: add a “Connectors” tab in the settings modal for storage providers.
- UI scope only: list S3, Dropbox, Google Drive; allow connect, verify connection, and delete connection from the UI. No backend wiring yet.
- Reuse the backup core registry/driver shape for provider metadata where helpful.
