# Frozen v1 product line; no Vercel project builds it. It carries the ~5GB
# Tauri target dir and Expo's 150MB native .so files, which exceed Vercel's
# 100MB per-file upload limit.
v1-final

# Build artifacts / caches
.next
apps/site/out
node_modules
**/node_modules
**/target
*.tsbuildinfo

# Local-only
.env
.env.*
!.env.example
.claude

# Local data/build dirs (CLI deploys)
.build
.data
