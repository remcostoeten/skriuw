{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the isolated desktop bridge benchmark.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
